open a shell with sudo priviledges
$sudo -s 

view contents of tar
$tar -tf filename.tar.gz

this didn't work
$vim filename.tar.gz

extract the file
$gzip -d filename.tar.gz

$tar -xvf filename.tar

compress the file / directory
$tar -cvzf file.tar.gz inputfile1 inputfile2


